# ADR-0007：默认使用 SSE 推送任务事件

- 状态：已接受
- 日期：2026-07-21

## 背景

生图耗时较长，浏览器需要持续获得任务阶段和终态，并在网络中断后恢复。

## 决策

创建任务默认返回 SSE；事件写入 `task_events` 并使用递增 ID，客户端通过 `Last-Event-ID` 恢复，多次失败后回退轮询。

## 备选方案

纯轮询实现简单但延迟和请求量更高；WebSocket 对单向任务事件不是必要条件。

## 影响

SSE 是任务级连接，不显示常驻“已连接”；浏览器断开不会取消后台任务。
