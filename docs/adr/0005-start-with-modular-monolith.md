# ADR-0005：首版采用模块化单体

- 状态：已接受
- 日期：2026-07-21

## 背景

首版需要快速形成用户、Provider、会话、任务、存储、统计和管理闭环，同时保留独立 Worker。

## 决策

API 使用按领域拆分的模块化单体；任务 Worker 复用同一后端 crate，Host Updater 因 Docker 权限边界单独部署。

## 备选方案

全面微服务可独立扩缩容，但会提前引入服务发现、分布式事务和运维复杂度。

## 影响

当前部署简单；未来只在真实容量或隔离需求出现时拆分服务。
