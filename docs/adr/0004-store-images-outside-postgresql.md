# ADR-0004：图片文件存储在 PostgreSQL 之外

- 状态：已接受
- 日期：2026-07-21

## 背景

上游临时 URL 会失效，大体积 Base64/二进制直接进入数据库会放大备份、复制和查询成本。

## 决策

原始图片持久化到 Local 或 S3；PostgreSQL 的 `image_assets` 只保存驱动、容器、Key、哈希、尺寸和业务关系。

## 备选方案

保存上游 URL 不可靠；使用 BYTEA 简化一致性但不利于容量和对象生命周期管理。

## 影响

任务只有在文件和元数据都成功后才完成，并需要补偿删除与一致性扫描。
